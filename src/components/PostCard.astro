---
const { post } = Astro.props;
---

<article class="lw-post-card">
  <header class="lw-post-header">
    <a href={`posts/${post.id}`} class="lw-post-title">
      {post.title}
    </a>
    <div class="lw-post-meta">
      <span class="lw-post-author">{post.username}</span>
      <time datetime={post.created_at} class="lw-post-date">
        {new Date(post.created_at).toLocaleDateString(undefined, { month: 'short', day: 'numeric', year: 'numeric' })}
      </time>
    </div>
  </header>

  
</article>

<style>
  /* Container: thin vertical spacing */
  .lw-post-card {
    max-width: 800px;              /* Narrower card */
  margin: 2rem 0 0rem 15rem;      /* Align left, reduce right margin */
  border-top: 1px solid #ddd;
  padding-top: 1rem;
}

/* Header: title left, meta right */
.lw-post-header {
  display: flex;
  justify-content: space-between;
  align-items: baseline;         /* Align bottom of title and meta */
  margin-bottom: 0.25rem;        /* Less vertical space */
}

.lw-post-title {
  font-family: 'Georgia', serif;
  font-size: 1.25rem;            /* Slightly smaller title */
  line-height: 1.1;
  color: #111;
  text-decoration: none;
}

.lw-post-title:hover {
  text-decoration: underline;
}

.lw-post-meta {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  font-family: 'Helvetica Neue', sans-serif;
  font-size: 0.75rem;
  color: #666;
}

.lw-post-author {
  font-weight: 500;
}

.lw-post-date {
  font-style: normal;
}

/* Optional: remove unused class */
.lw-post-excerpt {
  display: none;
}
</style>
